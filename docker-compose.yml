services:
  frontend:
    build: .
    ports:
      - '3000:3000'
    environment:
      - ENV_PORT=3000
    env_file:
      - .env.local
    networks:
      - pulisync
      
networks:
  pulisync:
    external: true
